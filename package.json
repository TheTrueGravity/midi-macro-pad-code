{
  "name": "midi-macro-pad",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && push",
    "start": "npm run build && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "midi": "^2.0.0",
    "voicemeeter-connector": "^1.0.3"
  }
}
